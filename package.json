{
  "name": "careerassistant-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "postinstall": "cd backend && npm install --no-audit --no-fund",
    "heroku-postbuild": "echo \"No frontend build\""
  },
  "engines": {
    "node": ">=18"
  }
}
